#include <bits/stdc++.h>
using namespace std;

int main()
{
	int a, b, kase;
	
	cin>>kase;
	for(int i=1; i<=kase; i++)
	{
		int sum=0;
		
		cin>>a;
		cin>>b;
		if(a%2==0)
			a++;
		if(b%2==0)
			b--;
			
		for(int j=a; j<=b; j+=2)
			sum+=j;
			
		cout<<"Case "<<i<<": "<<sum<<endl;
	}
	return 0;
}

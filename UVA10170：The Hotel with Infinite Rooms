#include <bits/stdc++.h>
using namespace std;

int main()
{
	long long s, d;
	
	while(cin>>s>>d)
	{
		long long area=0;
		
		for(int i=0;; i++)
		{
			area=(s+(s+i))*(i+1)/2;
			if(area>=d)
			{
				cout<<s+i<<endl;
				break;
			}
		}
	}
	return 0;
}
